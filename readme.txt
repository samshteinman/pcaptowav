Extracts raw USB data from USB wireshark trace

Adds a WaveFormat header to the data to turn it into playable audio(currently assumes data is PCM)

Requires:
Wireshark >= 3.0.7 & USBPcap >= 1.5.3
64-bit Powershell